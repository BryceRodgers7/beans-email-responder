# Copy to .env for local runs (.env is gitignored). In GitHub Actions these
# are provided as encrypted repo secrets instead of a file.

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Gmail OAuth (filled in during Phase 3 / 5 — see DESIGN.md sections 5 and 7)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
